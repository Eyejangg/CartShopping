export class UserState {
    public static NEW = "new";  // สถานะของผู้ใช้ใหม่
    public static ACTIVE = "active";  // สถานะของผู้ใช้ที่ยังใช้งานอยู่
    public static BLOCKED = "blocked";  // สถานะของผู้ใช้ที่ถูกบล็อค
    public static BANNED = "banned";  // สถานะของผู้ใช้ที่ถูกแบน
}
